[package]
name = "simple"
version = "0.1.0"
edition = "2024"

[dependencies]
luminal = { path = "../.." }
tikv-jemallocator = { version = "0.7", features = ["unprefixed_malloc_on_supported_platforms"] }
