; Forward LayoutTensor Propagation : Logical Exp
(rule
  (
    (= ?out_logical (LogicalExp ?in_logical))
    (= ?in_layout_tensor (LayoutTensorLit ?in_logical ?in_layout))
  )
  (
    (let new_out_layout_tensor (LayoutTensorLit ?out_logical ?in_layout))
  )
)
