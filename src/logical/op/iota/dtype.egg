;Dtype Propagation : LogicalIota — index data is always Int (luminal's
; iota dtype; the value IS an index expression, never a float).
(rule
  (
    (= ?out_logical_tensor (LogicalIota ?index_expression ?shape))
  )
  (
    (set (dtype-of ?out_logical_tensor) (Int))
  )
  :ruleset prop
)
