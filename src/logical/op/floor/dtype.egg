; DType Propagation : LogicalFloor — a rounding op preserves the element type.
(rule
  (
    (= ?out_logical_tensor (LogicalFloor ?in_logical))
    (= ?dtype (dtype-of ?in_logical))
  )
  (
    (set (dtype-of ?out_logical_tensor) ?dtype)
  )
  :ruleset prop
)
