; DType Propagation : LogicalCeil — a rounding op preserves the element type.
(rule
  (
    (= ?out_logical_tensor (LogicalCeil ?in_logical))
    (= ?dtype (dtype-of ?in_logical))
  )
  (
    (set (dtype-of ?out_logical_tensor) ?dtype)
  )
  :ruleset prop
)
