; Generic Constant : a ZERO-INPUT source op — writes its one scalar
; value into a rank-0 output. Value and output layout are metadata.
(constructor LayoutTensorOpConstantGeneric (f64 Layout) LayoutTensorOp)
